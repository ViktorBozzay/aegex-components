import { ChartCreationProperties } from '../types/ChartCreationProperties.js';

export const defaultChartProperties: ChartCreationProperties = {
  width: 628,
  height: 471,
  chartBackgroundColor: 'white',
  chartTitle: 'Manufacturing report',
  legendPosition: 'top',
};
